# 高価値帯算出アルゴリズム

1. インプレー打球を抽出

2. 各打球に V を付与

3. EV×LA を 2×2 ビンに分割

4. 各セルで E[V], count を計算

5. count < N_min のセルを除外

6. 閾値条件（V>=T or 上位q%）を満たすセル集合 Z を定義

7. 各打球に対し：

y₁ = 1 if (EV, LA) ∈ Z  
y₁ = 0 otherwise

